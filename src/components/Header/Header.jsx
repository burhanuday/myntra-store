import styled from "styled-components";

const Container = styled.header`
  padding: 1em 3em;
  box-shadow: 0px 0px 2px 0px #aaa;
`;

const Header = () => {
  return <Container>Myntra</Container>;
};

export default Header;
